steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - "-t"
      - "gcr.io/$PROJECT_ID/atbash-sandbox:0.3.18"
      - "--build-arg"
      - "ATBASH_CLI_VERSION=0.3.18"
      - "."
  - name: gcr.io/cloud-builders/docker
    args: ["push", "gcr.io/$PROJECT_ID/atbash-sandbox:0.3.18"]

images:
  - "gcr.io/$PROJECT_ID/atbash-sandbox:0.3.18"

options:
  logging: CLOUD_LOGGING_ONLY
